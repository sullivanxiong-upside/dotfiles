# GWF Repository Configuration
# This file defines repositories and their config files for worktree management

# Base branch (auto-detected if not set)
# BASE_BRANCH=main

# Repository definitions
# Format: REPO_<NAME>=/path/to/repo

# Main repositories
REPO_DATA_PIPELINES=~/repos/data-pipelines
REPO_CUSTOMER_DASHBOARD=~/repos/customer-dashboard

# Config files to copy for each repository
# Format: CONFIGS_<REPO_NAME>=(file1 file2 ...)
# Paths are relative to the repository root

CONFIGS_DATA_PIPELINES=(
  "src/jobs/datahub_llm_analysis_service/local-config.sullivanxiong.yaml"
  "src/jobs/datahub_llm_job_processor/local-config.sullivanxiong.yaml"
)

CONFIGS_CUSTOMER_DASHBOARD=(
  ".env.local"
)

# Repositories that require setup (will prompt for paths)
SETUP_REPOS=(data-pipelines customer-dashboard)
